[package]
name = "arcflow-script"
version = "0.1.0"
edition.workspace = true
description = "Safe script document model and compiler for ArcFlow."
publish = false

[dependencies]
serde.workspace = true
serde_json.workspace = true

[lib]
path = "src/lib.rs"
